# PTAB1039

```c++
//题目分析
设置三个数组;
第一个数组存储卖,第二个数组存储买;
第三个数组存储买的个数;统计零的个数;
//暴力求解
char sale[1000];
char buy[1000];
int y,z = 0;
int regester;
int temp1;
int temp2;

while((sale[y] = getchar()) != '\n') y++;
while((buy[z] = getchar()) !='\n') z++;
temp1 = y;
temp2 = z;

for(int m =0;m <= z;m++){
    for(int n = 0;n <= y;n++){
        if(buy[m] == sale[n]){
            break;
        }
        else{
            //这里的判断自己也不清晰是怎么写出来的
            if(n == y)&&buy[n] != sale[y]) register += 1;
        }
    }    
}
if(register == 0){
    std::cout<<"Yes"<<temp1-temp2;
}
else{
    std::cout<<"No"<<register;
}

```

