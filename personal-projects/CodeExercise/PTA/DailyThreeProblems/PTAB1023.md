# PTAB1023

```c++
//程序目的:
数尽可能小,1放在开头,中间数据按照顺序排列;
//程序输入
十个数字,数字总个数不超过50;
整数中间空格分隔;
顺序输入;
至少一个非零;//数据的限制需要考虑在程序内吗
//数据操作
int count[];

//输入处理
//循环条件
'\n'
getchar()-'0';
//输出处理
for(){//原来这里是循环,循环放在这里没必要
    //
    if(count[0]){
        while(){
            if(count[i]) number = i; break;
        }
        if(count[i]==1){
            cout<<number;
            cout[number] = 0;
            for(){
                cout<<Print(count[i],i);
            }
        }
        else{
            cout<<number;
            cout[number]--;
            for(){
                cout<<Print(count[i],i);
            }
        }
        }
    }
    else{
        while(){
            //这个条件语句可以保证第一个接触到的不是0
            if(count[i]) number = i; break;  
            cout<<Print[count[i],i];
        }
    }
}

//遍历输出函数
void Print(int number,int index){    
    while(number--){
        cout<<index;
    }
}
```

```c++
//答案采用边输入边输出的操作
//自己使用的是一个数组保存完所有数据之后再输出
//题目中的两个限制条件都没有体现
```



